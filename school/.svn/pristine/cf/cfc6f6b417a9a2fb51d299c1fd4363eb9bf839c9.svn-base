package com.dkt.school.apv.controller;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("apv")
public class MyApproveApplication {

	private static final Logger logger = Logger.getLogger(MyApproveApplication.class);
	
	@RequestMapping("/myApproveApplication")
	public String MyApproveTab() {
		
		return "apv/myApproveApplication";
	}
}
